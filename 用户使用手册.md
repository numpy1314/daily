

1.面向xx用软件快速迁移部署的跨平台硬件屏蔽增强技术软件用户使用手册




编  写                   
校  对                   
审  核                   
批  准                   



清华大学
2025年 09 月

1.引言
1.1.编写目的
本用户使用手册用于指导测试面向xx用软件快速迁移部署的跨平台硬件屏蔽增强技术课题的相关要求，是对本课题编写的文档，分别明确了
本测试大纲的编写目的主要为“面向xx用软件快速迁移部署的跨平台硬件屏蔽增强技术”(项目编号：xx)生成测试报告提供依据，同时为验收测试阶段的管理工作和技术工作提供指导。
1.2.名词
【列出手册中使用的专门术语的定义和缩写词的原意】
API	应用编程接口(Application Programming Interface) 
ARP	地址解析协议(Address Resolution Protocol) 
HTTP	超文本传输协议(Hypertext Transport Protocol)
ICMP	网络控制报文协议(Internet Control Message Protocol)
IP	互联网络协议(Internet Protocol)
TCP	传输控制协议(Transmission Control Protocol)
TELNET	远程登录服务协议(Telecommunication Network Protocol)
TFTP	简单文件传输协议(Trivial File Transfer Protocol)
UDP	用户数据报协议(User datagram Protocol)
GPIO	通用输入输出接口(General Purpose Input/Output)
CPU	中央处理器(Central Process Unit)
POSIX	可移植操作系统接口规范(Portable Operating System Interface Specification)
Hypervisor	虚拟机监控器，用于创建和运行虚拟机的软件或硬件层。
Type 1 Hypervisor	裸机型虚拟机监控器，直接运行在物理硬件上，如 VMware ESXi、Xen。
Type 2 Hypervisor	宿主型虚拟机监控器，运行在操作系统之上，如 VMware Workstation、VirtualBox。
Full Virtualization	完全虚拟化，无需修改客户操作系统，模拟完整硬件环境。
Paravirtualization	半虚拟化，客户操作系统需修改以配合虚拟化接口，提升性能。
Hardware-assisted Virtualization	硬件辅助虚拟化，依赖 CPU 提供的虚拟化扩展（如 Intel VT-x、AMD-V）。
VM Exit	虚拟机退出事件，指 CPU 从客户机模式切换回主机模式。
VM Entry	虚拟机进入事件，指 CPU 从主机模式切换到客户机模式。
VMM (Virtual Machine Monitor)	虚拟机监控器，等同于 Hypervisor，负责管理虚拟机的生命周期。
vCPU	虚拟 CPU，分配给虚拟机的逻辑处理器。
vNIC	虚拟网络接口卡，虚拟机使用的网络接口。
vSwitch	虚拟交换机，用于虚拟机之间的网络通信。
SR-IOV	单根 I/O 虚拟化，允许物理设备（如网卡）直接分配给多个虚拟机。
Nested Virtualization	嵌套虚拟化，允许在虚拟机中再次运行虚拟机。
Kernel	操作系统内核，管理硬件资源与系统调用的核心部分。
System Call	系统调用，用户空间程序请求内核服务的接口。
Interrupt	中断，硬件或软件发出的信号，打断当前 CPU 执行流程。
IRQ (Interrupt Request)	中断请求，硬件设备向 CPU 发出的信号编号。
ISR (Interrupt Service Routine)	中断服务例程，内核处理中断的函数。
Scheduler	调度器，决定哪个进程或线程在何时运行。
Context Switch	上下文切换，CPU 从一个任务切换到另一个任务的过程。
Preemption	抢占，强制当前任务暂停，让更高优先级任务运行。
Kernel Mode	内核态，CPU 的高权限模式，可访问所有硬件资源。
User Mode	用户态，CPU 的低权限模式，限制对硬件的直接访问。
Syscall Interface	系统调用接口，用户程序与内核交互的 API。
Memory Management Unit (MMU)	内存管理单元，负责虚拟地址到物理地址的转换。
Page Table	页表，内核用于管理虚拟内存的数据结构。
TLB (Translation Lookaside Buffer)	转译后备缓冲器，缓存虚拟地址到物理地址的映射。
Kernel Module	内核模块，可动态加载/卸载的内核扩展代码（如 Linux 的 .ko 文件）。
Device Driver	设备驱动程序，内核用于控制特定硬件的代码。
init_task	Linux 内核中的初始任务（PID 0），所有进程的祖先。
kthread	内核线程，运行在内核空间的线程，用于执行后台任务。
fork()	创建新进程的系统调用。
exec()	加载并执行新程序的系统调用。
kmalloc / kfree	Linux 内核中用于动态分配和释放内存的函数。
spinlock	自旋锁，内核中用于多核同步的一种锁机制。
semaphore	信号量，内核中用于进程间同步的机制。
mmap	内存映射系统调用，将文件或设备映射到内存。
swap	交换机制，将不常用的内存页换出到磁盘。

1.3.参考资料
【列出有关资料的作者、标题、编号、发表日期、出版单位或资料来源，可包括：a.项目的计划任务书、合同或批文；b.项目开发计划；C. 需求规格说明书；d.概要设计说明书；e。详细设计说明书；f.测试计划；g。手册中引用的其他资料、采用的软件工程标准或软件工程规范。】
序号	文档标识	文档名称
1	AX-HTS	XXX合同书

1.4.软件概述
1.4.1.目标
研发一款面向xx软件快速迁移部署的、高安全、高性能、支持跨平台硬件屏蔽增强的系统虚拟化软件原型。具体目标包括：
(1)支持国产化平台：能够在基于 x86-64 或 aarch64 架构的国产多核硬件设备上稳定运行，实现对国产硬件的有效适配与资源虚拟化。
(2)提升系统安全性：通过采用 Rust 安全编程语言进行核心开发，从语言层面消除内存安全和并发安全漏洞，有效避免传统虚拟化软件（如 KVM）中已公布的相关 CVE 漏洞，为上层应用提供安全可靠的隔离运行环境。
(3)优化系统性能：通过高性能虚拟化技术研究，显著提升虚拟机的启动速度、I/O 吞吐能力和实时性，关键性能指标（如镜像加载时间、延迟抖动、I/O吞吐量）要显著优于主流方案（如 KVM）。
(4)提供灵活可扩展的架构：采用模块化、柔性混合软件架构设计，实现组件间松耦合，支持系统功能的可持续演进和灵活扩展，满足民用智能无人系统、工业控制、云计算系统等不同场景的多样化需求。
1.4.2.功能
全面的虚拟化支持：
(1)处理器虚拟化：支持 CPU 虚拟化（vCPU），充分利用硬件辅助虚拟化技术（如 Intel VT-x, ARM VE）。
(2)内存虚拟化：实现内存管理单元（MMU）虚拟化，为每个虚拟机提供隔离、受保护的地址空间。
(3)I/O 虚拟化：支持多种 I/O 设备模型，包括设备模拟、Virtio 半虚拟化、中介传递设备以及设备直通（PCIe Pass-through），为虚拟机提供高性能的网络、存储等外设支持。
多架构与多客户操作系统支持：
(4)支持在 x86-64 和 aarch64 两种主流架构的硬件平台上运行。
能够运行 Linux 等主流客户操作系统，并支持其上的常用命令行软件（如 shell, iperf, busybox, lua, httpd, redis 等）。
灵活的部署模式：
(5)支持 Type-1 Hypervisor 模式：直接运行在物理硬件上，管理虚拟机（如在 ARM 平台启动多核 Linux）。
(6)支持 Type-1.5 Hypervisor 模式：在宿主 Linux 系统中加载，将宿主机降级为第一个虚拟机，并可创建和管理其他虚拟机。
丰富的设备驱动与组件：
(7)提供基于 Rust 语言开发的、针对国产飞腾派等硬件平台的各类设备驱动支持，包括但不限于：复位（reset）、时钟（clock）、GPIO、I2C、PinMux、PWM、SPI、定时器（timer）、UART、看门狗（watchdog）、DMA、GIC、USB、PCIe、网络控制器（IGB, FXMAC）等。
(8)包含一系列成熟、可复用的 OS 核心组件，如内存分配器（allocator）、文件系统（axfs）、同步原语（axsync）、任务调度（axtask）、系统调用表（syscall-table）等，保障系统核心功能的完备性。
(9)虚拟机生命周期管理：提供创建、启动、关闭、配置虚拟机等基本管理功能。

1.4.3.性能
1.4.3.1.时间特性
axvisor 在时间特性上的关键性能指标（与 KVM 对比）如下：
(1)虚拟机启动时间：加载执行包含常用软件的裁剪优化镜像的时间，相比 KVM 缩短至原时间的 60% 以下。这体现了更快的初始化、资源分配和镜像加载能力。
(2)实时性与延迟抖动：以 cyclictest 等工具测试，延迟抖动性能优于 KVM 50% 以上。这表明 axvisor 在中断响应、任务调度等方面具有更好的确定性，更能满足实时性要求高的场景。
(3)I/O 吞吐量：在存储或网络 I/O 方面，吞吐量性能优于 KVM 30% 以上。这得益于高效的 I/O 虚拟化架构（如 Virtio 优化、设备直通支持）。
(4)数据传输时间：通过 DMA 驱动等优化，减少了 CPU 介入，降低了数据传输的延迟。
1.4.3.2.灵活性
axvisor 在灵活性上的体现如下
(1)操作方式灵活性：支持通过命令行工具（如 axcli）进行灵活的虚拟机配置与管理。软件架构模块化，允许通过配置文件（如 TOML 格式）灵活定义虚拟机规格（CPU 数量、内存大小、设备映射等），无需修改代码即可适配不同需求。
(2)硬件平台适应性：能够适配不同的国产硬件平台（如飞腾派），并通过硬件抽象层（HAL）设计，为未来适配更多硬件奠定了基础。
(3)软件配置适应性：系统功能组件（如文件系统、调度算法）可通过 Cargo features 进行选择性编译和配置，满足不同场景下的功能剪裁和性能优化需求。
(4)架构演进灵活性：采用组件状态与控制逻辑分离的松耦合设计，使得各个模块能够独立进行演进、替换与升级，具备良好的可持续演进能力。

1.5.运行环境
1.5.1.硬件
axvisor 系统虚拟化软件运行时所需的最小硬件配置如下：
1.5.1.1.计算机型号、主存容量；
(1)处理器（CPU）：基于 x86-64 或 aarch64 架构的多核处理器。CPU 必须支持硬件虚拟化扩展（如 Intel VT-x 或 ARM VE）。

(2)内存（主存）：不小于 4GB。
1.5.1.2.外存储器、媒体、记录格式、设备型号及数量；
(1)存储器：存储容量不小于 32GB 的硬盘或固态硬盘。

(2)媒体：用于存储系统镜像、虚拟机镜像及数据。

(3)记录格式：支持常见的文件系统格式，如 ext4。

1.5.1.3.输入、输出设备；
(1)输出设备：支持串口输出，用于显示系统调试和运行信息。数量：2。

(2)输入设备：标准键盘。数量：1。

(3)显示设备：显示器。数量：1。（注：在某些嵌入式部署中，可能仅依赖串口输出）

1.5.1.4.数据传输设备及数据转换设备的型号及数量。
(1)网络设备：至少一个以太网卡，用于网络通信和管理。
(2)调试用途：上位机（测试机）通过串口（如 USB 转 TTL 模块）与目标硬件连接。支持软件

1.6.使用说明
1.6.1.安装和初始化
axvisor 以源代码形式分发，用户需要通过编译生成可执行的内核镜像文件。
1.6.1.1.操作命令与安装步骤：
1.6.1.1.1.获取源代码：
git clone --recurse-submodules https://github.com/arceos-hypervisor/axvisor.git
cd axvisor
# 根据目标平台切换相应分支，例如对于 RK3588 平台：# git checkout rk3588_jd4

1.6.1.1.2.配置编译环境： 
rustc --version
确保 Rust 工具链（指定 nightly 版本）和必要的依赖库已安装。

1.6.1.1.3.编译镜像： 
使用 make 命令进行编译，需指定目标平台和虚拟机配置。
示例（aarch64 RK3588）：
make ARCH=aarch64 PLATFORM=configs/platforms/aarch64-rk3588j-hv.toml SMP=4 VM_CONFIGS=configs/vms/linux-rk3588-aarch64-smp-vm1.toml:configs/vms/linux-rk3588-aarch64-smp-vm2.toml LOG=debug GICV3=y upload
编译成功后，将在指定目录（如 target/）下生成内核镜像文件（如 Image_root_aarch64_rk3588）和设备树文件（如 rk3588.bin）。
1.6.1.1.4.反馈信息及其含意：
编译过程会在终端输出详细信息。若出现 error，表示编译失败，需根据错误信息排查问题。若最终显示生成目标文件，则表示成功。
1.6.1.1.5.表明安装完成的测试实例：
将编译生成的镜像文件传输到目标硬件并启动，若能在串口终端看到 axvisor 的启动日志信息，即表明安装和初始化成功。
1.6.1.1.6.安装所需的软件工具：
Git、Make、Rust 工具链（rustc, cargo）、交叉编译工具链（如 aarch64-linux-gnu）。


1.6.1.2.输入
1.6.1.2.1.数据背景
数据来源：主要来自用户配置、虚拟机镜像文件、以及硬件设备产生的数据流。
存储媒体：配置文件存放于宿主机文件系统；虚拟机镜像可存放于硬盘或内存。
出现频度：配置数据在启动时读取一次；I/O 数据在虚拟机运行时持续产生。
限制和质量管理：配置文件需符合 TOML 格式规范；虚拟机镜像需为支持的格式。
1.6.1.2.2.数据格式
axvisor 的主要输入是虚拟机配置文件，采用 TOML 格式；以及DTS 格式设备树文件 - 用于描述硬件设备的层次结构和属性
1.6.1.2.3.输入举例
虚拟机配置文件举例
以 arceos-aarch64.toml 作为一个简单的例子来解释：

基本信息配置 ([base] 部分) 
id = 1 - 虚拟机唯一标识符
name = "arceos" - 虚拟机名称
vm_type = 1 - 虚拟化类型
cpu_num = 1 - 虚拟CPU数量
phys_cpu_sets = [1] - 分配给虚拟机的物理CPU集合

内核配置 ([kernel] 部分)
entry_point = 0x4008_0000 - 内核镜像入口点地址
image_location = "fs" - 镜像加载位置（"memory" 或 "fs"）
kernel_path = "arceos-aarch64.bin" - 内核镜像文件路径
kernel_load_addr = 0x4008_0000 - 内核加载地址

内存区域配置
内存区域以 [base_paddr, size, flags, map_type] 格式定义：
base_paddr - 物理基地址
size - 内存大小
flags - 内存权限标志（0x7 表示读写执行权限）
map_type - 映射类型（0=MAP_ALLOC, 1=MAP_IDENTICAL）

设备配置 ([devices] 部分)
emu_devices - 模拟设备列表
passthrough_devices - 直通设备列表，每个设备包含名称、虚拟地址、物理地址、长度和中断分配信息

DTS设备树文件格式举例
对于需要设备树的客户机（如 Linux），还需要对应的 .dts 文件来描述硬件设备， 通常定义了内存布局、中断控制器、串口、定时器等硬件设备的详细信息
以configs/vmsDTS 文件举例：
总体结构
DTS文件以版本声明开头，并遵循分层树结构，根节点包含全局属性和代表硬件设备的子节。

根节点定义关键属性：
地址和大小单元：定义地址和大小的编码方式
兼容字符串：标识机器类型
Interrupt parent：指向主中断控制器

内存描述
内存使用一个专用节点来描述，该节点指定了物理内存布局。该reg属性定义了内存基址 (0x80000000) 和大小 (0x40000000 = 1GB)，并将device_type = "memory"其标识为系统内存。

中断控制器
中断控制器在节点中有详细描述。该 GIC（通用中断控制器）节点包括：

分配器和 CPU 接口的多个寄存器范围
interrupt-controller将其标记为中断源的属性
#interrupt-cells = <0x03>指示中断说明符使用 3 个单元格
用于消息信号中断的嵌套 MSI 控制器

串行端口（UART）
串行端口实现为 PL011 UART 控制器，节点指定：
reg通过属性的基地址和寄存器范围
中断线分配
UART 和 APB 时钟的时钟参考
标识特定 UART 类型的兼容字符串

计时器
ARM 通用定时器在专用节点linux-qemu.dts:334-338中进行了描述。它定义了四种不同的定时器中断（安全/非安全物理/虚拟定时器），并包含always-on指示定时器在电源管理期间保持活动状态的属性。

附加硬件组件
DTS 文件还描述了其他重要的系统组件：
CPU 拓扑：在 cpus 节点中定义，具有详细的套接字/集群/核心层次结构
时钟源：用于外设定时的固定频率时钟
GPIO 控制器：PL061 GPIO 控制器，用于通用 I/O 
实时时钟：PL031 RTC 用于计时
VirtIO 设备：用于虚拟化 I/O 的多个内存映射 VirtIO 

设备启动配置
该节点包含启动时配置，包括内核命令行参数、控制台输出路径和安全功能的随机种子

1.6.1.3.输出
1.6.1.3.1.数据背景
去向：主要输出至串口控制台，用于系统状态监控、调试和故障诊断。部分输出（如性能测试结果）可能写入文件。

使用频度：系统运行期间持续输出日志信息。

存放媒体：串口输出；文件存储于虚拟机的文件系统中。

质量管理：日志信息分不同级别（如 INFO, DEBUG, ERROR），便于筛选。
1.6.1.3.2.数据格式
输出信息为文本格式，通常包含以下几部分：

首部：日志级别（如 [INFO]）、模块名（如 [AXVISOR]）。

主体：具体的描述性信息，如 "Booting on aarch64 platform...", "VM 'test' started successfully."。

尾部：通常以换行符结束。
1.6.1.3.3.输出举例
可查看https://github.com/arceos-hypervisor/axvisor/blob/master/README_CN.md
d8888                            .d88888b.   .d8888b.
      d88888                           d88P" "Y88b d88P  Y88b
     d88P888                           888     888 Y88b.
    d88P 888 888d888  .d8888b  .d88b.  888     888  "Y888b.
   d88P  888 888P"   d88P"    d8P  Y8b 888     888     "Y88b.
  d88P   888 888     888      88888888 888     888       "888
 d8888888888 888     Y88b.    Y8b.     Y88b. .d88P Y88b  d88P
d88P     888 888      "Y8888P  "Y8888   "Y88888P"   "Y8888P"

arch = aarch64
platform = aarch64-qemu-virt-hv
target = aarch64-unknown-none-softfloat
build_mode = release
log_level = info
smp = 1

[  0.020822 0 axruntime:130] Logging is enabled.
[  0.026419 0 axruntime:131] Primary CPU 0 started, dtb = 0x44000000.
[  0.028520 0 axruntime:133] Found physcial memory regions:
[  0.030673 0 axruntime:135]   [PA:0x40080000, PA:0x400d6000) .text (READ | EXECUTE | RESERVED)
[  0.033564 0 axruntime:135]   [PA:0x400d6000, PA:0x400ef000) .rodata (READ | RESERVED)
[  0.035313 0 axruntime:135]   [PA:0x400ef000, PA:0x400f5000) .data .tdata .tbss .percpu (READ | WRITE | RESERVED)
[  0.037083 0 axruntime:135]   [PA:0x400f5000, PA:0x40135000) boot stack (READ | WRITE | RESERVED)
[  0.038622 0 axruntime:135]   [PA:0x40135000, PA:0x4013b000) .bss (READ | WRITE | RESERVED)
[  0.040643 0 axruntime:135]   [PA:0x4013b000, PA:0x48000000) free memory (READ | WRITE | FREE)
[  0.042907 0 axruntime:135]   [PA:0x9000000, PA:0x9001000) mmio (READ | WRITE | DEVICE | RESERVED)
[  0.045011 0 axruntime:135]   [PA:0x9040000, PA:0x9041000) mmio (READ | WRITE | DEVICE | RESERVED)
[  0.047070 0 axruntime:135]   [PA:0x9100000, PA:0x9101000) mmio (READ | WRITE | DEVICE | RESERVED)
[  0.049093 0 axruntime:135]   [PA:0x8000000, PA:0x8020000) mmio (READ | WRITE | DEVICE | RESERVED)
[  0.051081 0 axruntime:135]   [PA:0xa000000, PA:0xa004000) mmio (READ | WRITE | DEVICE | RESERVED)
[  0.053120 0 axruntime:135]   [PA:0x10000000, PA:0x3eff0000) mmio (READ | WRITE | DEVICE | RESERVED)
[  0.055229 0 axruntime:135]   [PA:0x4010000000, PA:0x4020000000) mmio (READ | WRITE | DEVICE | RESERVED)
[  0.057642 0 axruntime:208] Initialize global memory allocator...
[  0.059377 0 axruntime:209]   use TLSF allocator.
[  0.072071 0 axmm:60] Initialize virtual memory management...
[  0.136312 0 axruntime:150] Initialize platform devices...
[  0.137733 0 axhal::platform::aarch64_common::gic:67] Initialize GICv2...
[  0.143653 0 axtask::api:73] Initialize scheduling...
[  0.151435 0 axtask::api:79]   use FIFO scheduler.
[  0.152744 0 axruntime:176] Initialize interrupt handlers...
[  0.157472 0 axruntime:186] Primary CPU 0 init OK.
[  0.159027 0:2 axvisor:17] Starting virtualization...
[  0.160968 0:2 axvisor:19] Hardware support: true
[  0.168619 0:4 axvisor::vmm::timer:103] Initing HV Timer...
[  0.170399 0:4 axvisor::hal:117] Hardware virtualization support enabled on core 0
[  0.295531 0:2 axvisor::vmm::config:33] Creating VM [1] "arceos"
[  0.301423 0:2 axvm::vm:113] Setting up memory region: [0x40000000~0x41000000] READ | WRITE | EXECUTE
[  0.334424 0:2 axvm::vm:156] Setting up passthrough device memory region: [0x8000000~0x8050000] -> [0x8000000~0x8050000]
[  0.339431 0:2 axvm::vm:156] Setting up passthrough device memory region: [0x9000000~0x9001000] -> [0x9000000~0x9001000]
[  0.341925 0:2 axvm::vm:156] Setting up passthrough device memory region: [0x9010000~0x9011000] -> [0x9010000~0x9011000]
[  0.343758 0:2 axvm::vm:156] Setting up passthrough device memory region: [0x9030000~0x9031000] -> [0x9030000~0x9031000]
[  0.345559 0:2 axvm::vm:156] Setting up passthrough device memory region: [0xa000000~0xa004000] -> [0xa000000~0xa004000]
[  0.348819 0:2 axvm::vm:191] VM created: id=1
[  0.350749 0:2 axvm::vm:206] VM setup: id=1
[  0.352526 0:2 axvisor::vmm::config:40] VM[1] created success, loading images...
[  0.355270 0:2 axvisor::vmm::images:24] Loading VM[1] images from memory
[  0.363583 0:2 axvisor::vmm:29] Setting up vcpus...
[  0.368014 0:2 axvisor::vmm::vcpus:176] Initializing VM[1]'s 1 vcpus
[  0.370802 0:2 axvisor::vmm::vcpus:207] Spawning task for VM[1] Vcpu[0]
[  0.374805 0:2 axvisor::vmm::vcpus:219] Vcpu task Task(5, "VM[1]-VCpu[0]") created cpumask: [0, ]
[  0.378878 0:2 axvisor::vmm:36] VMM starting, booting VMs...
[  0.380775 0:2 axvm::vm:273] Booting VM[1]
[  0.382631 0:2 axvisor::vmm:42] VM[1] boot success
[  0.387436 0:5 axvisor::vmm::vcpus:240] VM[1] Vcpu[0] waiting for running
[  0.390048 0:5 axvisor::vmm::vcpus:243] VM[1] Vcpu[0] running...

       d8888                            .d88888b.   .d8888b.
      d88888                           d88P" "Y88b d88P  Y88b
     d88P888                           888     888 Y88b.
    d88P 888 888d888  .d8888b  .d88b.  888     888  "Y888b.
   d88P  888 888P"   d88P"    d8P  Y8b 888     888     "Y88b.
  d88P   888 888     888      88888888 888     888       "888
 d8888888888 888     Y88b.    Y8b.     Y88b. .d88P Y88b  d88P
d88P     888 888      "Y8888P  "Y8888   "Y88888P"   "Y8888P"

arch = aarch64
platform = aarch64-qemu-virt
target = aarch64-unknown-none-softfloat
build_mode = release
log_level = warn
smp = 1

Hello, world!
[  0.416823 0:5 axvisor::vmm::vcpus:288] VM[1] run VCpu[0] SystemDown
[  0.419035 0:5 axhal::platform::aarch64_common::psci:98] Shutting down...

AxVisor主机启动阶段：显示ArceOS的启动logo、架构信息、内存区域初始化、设备初始化等
虚拟机创建阶段：创建VM[1] "arceos"、设置内存区域、配置直通设备、加载镜像
虚拟CPU初始化：设置vCPU、创建任务线程
客户机启动阶段：客户机(ArceOS)成功启动，显示自己的logo和"Hello, world!"输出
正常关机：虚拟机执行完毕后正常关机
1.6.1.4.出错和恢复
1.6.1.4.1.VCpu执行错误
错误消息：
"vcpu_on: set_entry failed" 
"VM shutdown failed" 
"VM[{}] run VCpu[{}] get error {:?}" 
"VM[{}] VCpu[{}] run failed with exit code {}"

含义：
当虚拟机管理程序无法设置虚拟 CPU 的初始执行地址时，会发生 VCpu 入口点设置失败
当系统无法正确终止虚拟机时，就会发生虚拟机关闭失败
VCpu 执行错误表示虚拟 CPU 运行时执行过程中出现问题
硬件输入失败表明底层虚拟化硬件支持存在问题

故障排除：
验证入口点地址是否有效且在虚拟机的内存空间内
检查硬件虚拟化是否已正确启用并支持
确保 VM 配置指定正确的内存区域和 CPU 设置
1.6.1.4.2.镜像加载错误
错误消息：
"Failed to load VM images"
"Failed to load DTB images" 
"Failed to load BIOS images"
"BIOS load addr is missed" 
"Ramdisk load addr is missed"
"DTB load addr is missed" 
"Failed in reading from file {}, err {:?}" 
"Failed to open {}, err {:?}, please check your disk.img"

含义：
当系统无法将内核、DTB 或 BIOS 映像加载到虚拟机内存中时，就会发生映像加载失败
缺少加载地址错误表示配置不完整，未指定所需的内存地址
文件访问错误表明文件系统或图像文件路径存在问题

故障排除：
验证配置中的所有镜像文件路径是否正确且可访问
确保在虚拟机配置文件中正确指定加载地址
检查磁盘映像是否正确安装并包含所需的文件
验证文件权限和文件系统完整性

1.6.1.4.3.虚拟机配置错误
错误消息：
"Failed to resolve VM config" 
"Failed to create VM" 
"Failed to load VM images"

含义：
当 TOML 配置文件包含无效语法或缺少必填字段时，配置解析失败
当虚拟机管理程序无法分配资源或初始化虚拟机时，就会发生虚拟机创建失败
初始化期间图像加载失败表明配置的图像路径或格式存在问题

故障排除：
验证 TOML 配置文件语法并确保所有必填字段都存在
检查主机系统上的内存和 CPU 资源可用性
验证 VM 配置是否指定了支持的硬件功能
确保镜像路径和加载地址配置正确

1.6.1.4.4.硬件虚拟化错误
错误消息：
"Initialize CPU affinity failed!" 
"Failed to initialize percpu state" 
"Failed to enable virtualization" 

含义：
当系统无法将虚拟 CPU 绑定到特定的物理 CPU 核心时，就会发生 CPU 亲和性故障
每个 CPU 状态初始化失败表明各个核心上的硬件虚拟化设置存在问题
虚拟化启用失败表明硬件虚拟化功能不可用或无法访问

故障排除：
验证 BIOS/UEFI 设置中是否启用了硬件虚拟化
检查主机 CPU 是否支持所需的虚拟化扩展（VT-x、AMD-V 等）
确保虚拟机管理程序具有足够的权限来访问虚拟化硬件
验证没有其他虚拟机管理程序正在使用虚拟化功能

1.6.1.4.5.VM启动错误
错误消息：
"VM[{}] boot failed, error {:?}" 

含义：
当虚拟机无法启动执行时，就会发生虚拟机启动失败，这通常是由于配置问题或资源限制造成的

故障排除：
检查虚拟机配置以获取正确的入口点和内存布局
验证所有必需的映像（内核、DTB、BIOS）是否已正确加载
确保有足够的主机系统资源可用
查看日志消息以获取更具体的错误详细信息

1.6.1.4.6.构建时配置错误
错误消息：
"Failed to read file {}: {}" 
“compile_error!(\"{}\")"

含义：
当编译期间无法读取或解析指定的 VM 配置文件时，会发生构建时配置错误

故障排除：
验证AXVISOR_VM_CONFIGS环境变量是否指向有效的配置文件
确保所有引用的配置文件都存在且可读
检查配置文件语法是否为有效的 TOML 格式

1.6.1.4.7.虚拟机管理错误
错误消息：
"VM[{}] already exists, push VM failed, just return ..."

含义：
尝试创建具有相同标识符的多个虚拟机时发生虚拟机 ID 冲突

故障排除：
确保配置中的每个虚拟机都有唯一的 ID
检查虚拟机配置文件中是否存在重复的 ID 分配

1.6.1.4.8.不支持的功能
错误消息：
"Check your \"image_location\" in config.toml, \"memory\" and \"fs\" are supported,\n NOTE: \"fs\" feature should be enabled if you want to load images from filesystem. (APP_FEATURES=fs)" 

含义：
使用不受支持的图像加载方法或未启用所需功能时，会出现功能支持错误

故障排除：
仅使用受支持的 image_location 值：“memory”或“fs”
如果从文件系统加载图像，请启用“fs”功能：APP_FEATURES=fs
查看配置文档以了解支持的选项

1.7.运行说明
1.7.1.运行表
运行情况	运行目的
Type-1.5 模式 	在宿主 Linux 中加载 axvisor，将宿主机降级为第一个虚拟机，并创建和管理其他虚拟机。用于在通用服务器上实现虚拟化。
Type-1 模式 	axvisor 直接运行在国产硬件（如 RK3588）上，作为裸机 Hypervisor 启动和管理多个虚拟机（如 Linux）。用于嵌入式或特定硬件环境。

1.7.2.运行步骤
1.7.2.1.运行控制
运行通过 U-Boot 引导程序或宿主 Linux 的 axcli 工具进行控制。

1.7.2.2.操作信息
1.7.2.2.1.在 x86_64 上以 Type-1.5 模式运行
操作要求 ：HostOS Linux 已安装，CPU 支持 VT-x。

启动方法：
在宿主 Linux 中，插入 axvisor 内核模块。
使用 axcli 工具启用虚拟化并创建虚拟机。

预计运行时间：秒级。

操作命令格式及格式说明 ：
加载内核模块
sudo insmod /path/to/axvisor.ko
启用虚拟化
sudo ./axcli enable
创建并启动虚拟机
sudo ./axcli vm create --name <vm_name> --cfg-path <config.toml>
其他事项 ：需提前准备好虚拟机配置文件和外设镜像，若启动的guestVM 为Linux，需要为其制作ramdisk作为rootfs。

1.7.2.2.2.在 aarch64 (RK3588) 上以 Type-1 模式运行
操作要求 ：硬件平台为 ROC-RK3588s-PC，镜像已烧录或置于启动介质。

启动方法 ：
启动开发板，在 U-Boot 阶段中断引导。
在 U-Boot 命令行下，加载 axvisor 镜像和设备树并启动。
预计运行时间 ：数十秒。
操作命令格式及格式说明 (U-Boot 命令行下)：
bash
# 从存储介质加载镜像和设备树到内存
load mmc 0:8 0x4000000 /boot/Image_root_aarch64_rk3588
load mmc 0:8 0x10000000 /boot/rk3588.bin# 启动 axvisor
bootm 0x4000000 - 0x10000000
其他事项 ：需通过串口连接查看启动日志。

1.7.2.2.3.输入/输出文件
文件的名称及编号：
Image_root_aarch64_rk3588：axvisor 内核镜像文件。
rk3588.bin：设备树二进制文件。
linux-x86_64.toml：虚拟机配置文件示例。
guest-kernel-image：客户操作系统内核镜像。
记录媒体：硬盘、SD 卡或 eMMC。
存留的目录：通常位于启动分区的 /boot/ 目录或用户指定目录。
文件的支配：系统镜像文件为关键文件，需妥善保管。日志文件可根据需要清理。

1.7.2.2.4.启动或恢复过程
启动：如上所述，通过引导加载器或命令行工具启动。
恢复：若系统崩溃，需硬件断电后重新启动。对于虚拟机内部问题，可通过 axvisor 的管理接口重启虚拟机。

1.8 （未完成）非常规过程
    【提供应急或非常规操作的必要信息及操作步骤，如出错处理操作、向后备系统切换操作以及维护人员须知的操作和注意事项。】

1.9 （未完成）操作命令一览表
    【按字母顺序逐个列出全部操作命令的格式、功能及参数说明。】

1.10 （未完成）程序文件（或命令文件）和数据文件一览表
    【按文件名字母顺序或按功能与模块分类顺序逐个列出文件名称、标识符及说明。】

1.11 （未完成）用户操作举例

1.12.核心crate的API接口及说明
1.12.1.Axvisor
1.12.1.1.VMM核心API
1.12.1.1.1.初始化流程与系统启动时序
1.12.1.1.1.1.vmm::init() 函数
该函数负责初始化VMM环境，创建所有虚拟机结构并为每个虚拟机设置主vCPU。
函数签名: pub fn init()
功能描述:
i.调用 config::init_guest_vms() 根据配置文件初始化客户虚拟机。
ii.遍历虚拟机列表，调用 vcpus::setup_vm_primary_vcpu(vm) 为每个虚拟机设置主vCPU并生成相应的任务。
执行时机: 在内核启动后由 main.rs 中的 main() 函数调用。
1.12.1.1.1.2.vmm::start() 函数
该函数启动VMM并引导所有已配置的虚拟机运行。
函数签名: pub fn start()
功能描述:
i.遍历虚拟机列表，调用每个虚拟机的 boot() 方法尝试启动。
ii.若启动成功，则通过 vcpus::notify_primary_vcpu(vm.id()) 通知主vCPU开始执行，并递增运行中的虚拟机计数。
iii.使用等待队列阻塞主线程，直到所有虚拟机停止运行。
同步机制: 利用原子变量 RUNNING_VM_COUNT 和等待队列 VMM 实现多虚拟机运行状态的监控与退出控制。
1.12.1.1.2.虚拟机列表管理接口
1.12.1.1.2.1.接口概述
vm_list.rs 提供全局线程安全的虚拟机列表管理功能，基于 BTreeMap<usize, VMRef> 存储，通过自旋锁保护访问。
1.12.1.1.2.2.push_vm(vm: VMRef)
功能: 将指定虚拟机添加到全局列表。
参数: vm - 虚拟机引用对象。
行为: 若ID已存在则记录警告并不插入。
线程安全: 使用 Mutex 保证并发安全。
1.12.1.1.2.3.get_vm_by_id(vm_id: usize) -> Option<VMRef>
功能: 根据虚拟机ID获取其引用。
参数: vm_id - 目标虚拟机唯一标识符。
返回值: 成功返回 Some(VMRef)，否则 None。
1.12.1.1.2.4.get_vm_list() -> Vec<VMRef>
功能: 获取当前所有虚拟机的克隆引用列表。
用途: 用于批量操作或遍历所有运行中虚拟机。
1.12.1.1.3.定时器管理与时间虚拟化
1.12.1.1.3.1.模块职责
timer.rs 实现VMM级别的定时器管理，支持高精度定时事件注册与回调处理，服务于虚拟机时间虚拟化需求。
1.12.1.1.3.2.init_percpu()
功能: 初始化每CPU核心的定时器列表。
调用位置: 在 hal::enable_virtualization() 中为每个核心调用。
数据结构: 使用 LazyInit<SpinNoIrq<TimerList<VmmTimerEvent>>> 实现每核惰性初始化。
1.12.1.1.3.3.register_timer<F>(deadline: u64, handler: F) -> usize
功能: 注册一个在指定截止时间触发的定时器。
参数:
odeadline: 绝对时间戳（纳秒）。
ohandler: 一次性回调函数。
返回值: 唯一令牌（token），可用于后续取消。
内部机制: 将用户闭包包装为 VmmTimerEvent 并插入红黑树组织的定时器队列。
1.12.1.1.3.4.cancel_timer(token: usize)
功能: 取消已注册的定时器。
参数: token - 由 register_timer 返回的唯一标识。
1.12.1.1.3.5.check_events()
功能: 检查并处理所有到期的定时器事件。
调用时机: 通常在中断上下文或调度点调用。
执行方式: 循环调用 expire_one() 直至无更多到期事件。
1.12.1.2.VCPU管理API
1.12.1.2.1.vCPU创建与任务初始化
vCPU的创建过程由setup_vm_primary_vcpu函数启动，该函数为虚拟机设置主vCPU并初始化其等待队列和任务列表。系统首先通过VMVCpus::new创建一个包含等待队列、vCPU任务列表和运行计数器的结构体，然后为每个vCPU分配一个Arceos任务。
vCPU任务的分配通过alloc_vcpu_task函数完成，该函数创建一个以vcpu_run为入口函数的任务，并为其分配256 KiB的内核栈空间。如果vCPU有指定的物理CPU集，则会初始化相应的CPU掩码。任务扩展数据TaskExt被初始化以包含对VM和vCPU的引用，确保任务能够访问其所属的虚拟机和虚拟CPU资源。
1.12.1.2.2.vCPU运行循环与退出处理
vCPU的主要执行流程在vcpu_run函数中实现，这是一个无限循环，持续运行vCPU并处理各种退出原因。当vCPU首次启动时，它会等待虚拟机进入运行状态，然后标记自己为正在运行。
运行循环的核心是vm.run_vcpu(vcpu_id)调用，该调用返回不同的退出原因，包括超调用、外部中断、暂停、CPU下线等。对于每种退出原因，都有相应的处理逻辑：超调用会被转发给HyperCall处理器；外部中断会触发中断处理程序；暂停状态会使vCPU进入等待状态；而系统关闭信号会导致虚拟机关闭。
当检测到虚拟机正在关闭时，vCPU会调用mark_vcpu_exiting减少运行计数，如果是最后一个退出的vCPU，则会减少运行中的虚拟机总数并唤醒VMM等待队列。
1.12.1.2.3.VCpuRef安全访问模式
VCpuRef类型通过Arc智能指针实现线程安全的共享访问，确保多个线程可以安全地引用同一个vCPU实例。系统通过with_vcpu_task函数提供了一种安全的访问模式，该函数接受一个闭包并在获取到vCPU任务引用后执行该闭包。
这种设计模式避免了直接暴露vCPU引用可能带来的竞态条件，而是通过受控的访问路径来操作vCPU资源。TaskExt结构体作为任务扩展数据，包含了对VM和VCPU的引用，这些引用在任务创建时被初始化，并在整个任务生命周期内保持有效。
跨线程传递遵循Rust的所有权规则，通过克隆Arc引用来实现共享所有权，而不是转移所有权。这种方式既保证了内存安全，又实现了高效的资源共享。
1.12.1.2.4.HAL层寄存器上下文保存与恢复
不同平台下的vCPU寄存器上下文保存与恢复机制存在显著差异。在ARM64架构中，inject_interrupt函数负责将虚拟中断注入到vCPU，它通过GIC驱动程序操作虚拟中断控制器。对于GICv2，使用gic.hypervisor_interface()获取超管理模式接口并设置虚拟中断；对于GICv3，则直接操作ICH寄存器。
x86_64架构的实现相对简单，inject_interrupt函数目前为空实现，表明该平台的中断注入机制可能在其他地方实现或尚未完全支持。这种差异反映了不同硬件架构在虚拟化支持方面的特性：ARM64需要显式管理虚拟中断控制器状态，而x86_64可能依赖于更底层的硬件辅助虚拟化特性。
1.12.1.2.5.多核SMP启动序列
多核SMP场景下的vCPU启动序列始于主vCPU的初始化，随后通过CpuUp退出原因触发次级vCPU的启动。当一个vCPU收到CpuUp信号时，它会解析目标物理CPU ID，查找对应的vCPU ID，然后调用vcpu_on函数启动目标vCPU。
启动过程包括验证vCPU处于空闲状态、设置入口点和参数、分配任务并将其添加到VM的vCPU任务列表中。系统通过vcpu_mappings配置获取vCPU与物理CPU的映射关系，确保正确的亲和性设置。每个新启动的vCPU都会经历相同的初始化流程，包括延迟启动、等待VM运行状态和标记为运行中。
1.12.1.2.6.异常注入与拦截接口
异常注入接口主要通过inject_interrupt函数族实现，允许hypervisor向特定vCPU注入虚拟中断。在ARM64平台上，这涉及复杂的GICv2/v3寄存器操作，包括查找空闲列表寄存器、设置虚拟中断ID和状态位。系统会检查中断是否已挂起或激活，避免重复注入。
异常拦截则通过vCPU运行循环中的退出原因处理实现。当发生外部中断时，系统调用axhal::irq::irq_handler处理中断并向量分发。发送IPI（处理器间中断）时，如果目标是当前vCPU，则直接注入中断；否则通过vm.inject_interrupt_to_vcpu向目标vCPU发送中断。
这些接口共同构成了完整的异常处理框架，使hypervisor能够精确控制虚拟机的中断行为，实现高效的虚拟化性能。
1.12.1.3.硬件抽象层API
1.12.1.3.1.异常向量与中断控制器访问接口
HAL层通过api.rs暴露架构特定的原语，用于异常向量安装和中断控制器访问。在AArch64平台上，这些接口直接与GIC（通用中断控制器）驱动交互，支持GICv2和GICv3两种模式。
关键接口包括：
hardware_inject_virtual_interrupt：向虚拟CPU注入中断
read_vgicd_typer：读取虚拟GIC分发器TYPER寄存器
read_vgicd_iidr：读取IIDR寄存器以识别GIC版本
get_host_gicd_base 和 get_host_gicr_base：获取物理GIC寄存器基地址
中断注入流程涉及查找空闲列表寄存器（List Register），检查状态位，并写入虚拟中断ID和状态字段。若无空闲LR，则尝试复用无效条目或触发panic。
1.12.1.3.2.缓存管理API跨平台抽象设计
缓存管理API在cache.rs中实现了跨平台抽象，定义了统一的操作枚举CacheOp，包含清理（Clean）、无效化（Invalidate）和清理并无效化（CleanAndInvalidate）三种操作。
1.12.1.3.2.1.AArch64实现
在AArch64架构下，dcache_range函数将CacheOp转换为底层CPU扩展库中的对应操作，并调用aarch64_cpu_ext::cache::dcache_range执行实际的缓存操作。该实现充分利用了ARM架构提供的数据缓存维护指令。
1.12.1.3.2.2.x86_64实现
相比之下，x86_64平台的dcache_range为空实现，参数被标记为未使用（_op, _addr, _size）。这表明当前系统假设x86_64平台的缓存一致性由硬件自动管理，无需显式软件干预。
1.12.1.3.3.内存屏障与原子操作保证
HAL层通过依赖ArceOS内核模块中的axhal组件来保证内存屏障与原子操作的正确性。虽然具体实现未在当前代码片段中展示，但其设计原则是利用各架构提供的内存屏障指令（如AArch64的DSB, DMB和x86_64的MFENCE）以及原子指令（如LDXR/STXR或CMPXCHG）来确保多核环境下的数据一致性。
在虚拟化上下文中，这些原语对于同步虚拟机监控器与客户操作系统之间的共享数据结构至关重要，尤其是在IVC（Inter-VM Communication）通道建立和超调用处理过程中。
1.12.1.3.4.新平台HAL接口实现契约
要在新平台上实现HAL接口，必须遵循以下契约要求：
1.提供架构特定模块：在src/hal/arch/<new_arch>/mod.rs中实现必要的函数，如hardware_check、inject_interrupt等。
2.实现缓存操作：根据目标架构特性，在cache.rs中实现dcache_range函数，或将不支持的操作留空。
3.中断控制器集成：确保能够访问并操作本地中断控制器（如APIC、GIC等），提供读取寄存器和注入中断的能力。
4.硬件兼容性检查：在hardware_check函数中验证关键硬件特性（如页表级数、虚拟化支持等）是否满足运行时需求。
5.静态调度表注册：使用#[axvisor_api::api_mod_impl]宏将新实现的接口绑定到对应的API模块上，确保调用能正确路由。
此外，所有平台都应保持对AxVMHal、AxMmHal和AxVCpuHal等核心trait的一致实现，以维持上层VMM逻辑的可移植性。
1.12.1.4.通信机制API
1.12.1.4.1.请求处理与服务分发
execute()方法根据不同的HyperCallCode值分发至相应的处理分支，当前支持以下四种核心hypercall编号及其功能：
超调用编号	功能接口	输入参数格式	输出参数格式	安全边界
HIVCPublishChannel	发布一个IVC共享内存通道	[key: usize, shm_base_gpa_ptr: GPA*, shm_size_ptr: usize*]	写入shm_base_gpa_ptr返回分配的GPA，写入shm_size_ptr返回实际大小	仅允许VM自身发布通道；共享内存大小限制为4KB以内
HIVCUnPublishChannel	撤销已发布的IVC通道	[key: usize]	无输出	必须由原发布者VM调用；若存在订阅者则仅标记为不可用
HIVCSubscribChannel	订阅另一VM发布的IVC通道	[publisher_vm_id: usize, key: usize, shm_base_gpa_ptr: GPA*, shm_size_ptr: usize*]	写入指针返回映射后的本地GPA和大小	需目标VM已发布对应key的通道；需有足够的地址空间用于映射
HIVCUnSubscribChannel	取消对远程IVC通道的订阅	[publisher_vm_id: usize, key: usize]	无输出	必须是有效的订阅关系才能取消
1.12.1.4.2.跨虚拟机通信ivc通道机制
ivc.rs模块提供了完整的跨虚拟机通信基础设施，基于全局静态哈希表管理所有活动的IVC通道，并提供线程安全的操作接口。
1.12.1.4.2.1.IVC通道建立流程
IVC通信采用“发布-订阅”模型：
1.发布者调用HIVCPublishChannel，指定唯一key标识通道。
2.宿主机为其分配一页物理内存作为共享区域，并记录其宿主物理地址（HPA）和客户机物理地址（GPA）。
3.通道元数据注册至全局IVC_CHANNELS映射中，键为(publisher_vm_id, key)。
4.订阅者调用HIVCSubscribChannel，提供发布者ID和key。
5.宿主机查找对应通道，为订阅者分配本地GPA并将映射关系加入该通道的subscriber_vms列表。
6.双方通过映射到各自地址空间的同一块物理内存进行数据交换。
1.12.1.4.2.2.数据帧格式
每个IVC通道的共享内存起始处包含一个固定格式的头部IVCChannelHeader，其布局如下：
#[repr(C)]
struct IVCChannelHeader {
    publisher_id: u64,  // 发布者VM ID
    key: u64,           // 通道唯一标识
}
紧随其后的是可变长度的数据区域，供应用程序自定义协议使用。此设计保证了通信双方的身份可验证性。
1.12.1.4.3.读写同步原语与中断触发机制
IVC本身不提供内置的同步机制，依赖客户机操作系统实现基于轮询或中断的通知机制：
写操作完成后，发布者可通过其他手段（如虚拟中断）通知订阅者有新数据到达。
读操作前，订阅者应检查头部信息确认数据完整性。
实际中断触发逻辑通常集成在设备模拟层或专用中断控制器中，不在ivc.rs直接实现。
当最后一个订阅者取消订阅且通道已被撤销时，底层共享内存页会被自动释放（通过Drop trait）。
1.12.1.4.4.客户机镜像加载与ivc初始化
images/mod.rs模块负责客户机内核及其他镜像文件的加载，虽然它本身不直接初始化IVC通道，但为IVC的运行奠定了基础环境。
1.12.1.4.4.1.镜像加载流程
1.ImageLoader::load()根据配置决定从内存还是文件系统加载镜像。
2.支持加载内核、BIOS、DTB（设备树）和ramdisk等组件。
3.所有镜像被复制到客户机的主内存区域（VMMemoryRegion），并通过缓存清理确保一致性。
1.12.1.4.4.2.IVC端点的暴露时机
IVC通道并非在镜像加载阶段创建，而是在客户机操作系统启动后，通过执行特定hypercall指令动态建立。典型流程如下：
1.客户机操作系统完成基本初始化。
2.用户空间服务或驱动程序决定与其他VM通信。
3.通过SBI（RISC-V）或SMCCC（AArch64）等标准接口陷入宿主机，发起HIVCPublishChannel或HIVCSubscribChannel调用。
4.宿主机执行相应逻辑，完成通道建立或订阅。
因此，images/mod.rs的作用是确保客户机具备执行这些hypercall的能力——即正确加载了支持IVC协议的内核或用户程序。
1.12.1.4.5.安全边界与权限校验
整个通信机制设计遵循最小权限原则，关键安全措施包括：
调用码验证：HyperCall::new()确保只有预定义的HyperCallCode值被接受。
所有权检查：HIVCUnPublishChannel只能由原始发布者调用。
访问控制：HIVCSubscribChannel要求明确指定发布者VM ID和密钥，防止任意访问。
资源隔离：每个IVC通道的共享内存独立分配，避免越界访问。
生命周期管理：利用Rust的RAII机制，在IVCChannel析构时自动释放物理内存。
此外，共享内存映射时设置了READ \| WRITE权限标志，禁止执行权限，防范代码注入攻击。

1.12.1.5.配置管理API
1.12.1.5.1.核心组件
AxVMCrateConfig是虚拟机配置的核心数据结构，由AxVMConfig和其他子配置组成。它通过from_toml方法从TOML字符串解析而来，并在初始化过程中被转换为运行时可用的AxVMConfig。PassThroughDeviceConfig用于描述直通设备，而VmMemMappingType则定义了内存区域的映射方式（分配或直接映射）。
1.12.1.5.2.架构概述
系统启动时，build.rs根据环境变量AXVISOR_VM_CONFIGS读取TOML配置文件，将其内容嵌入到生成的vm_configs.rs中。运行时，config::static_vm_configs()函数返回这些预加载的配置字符串。init_guest_vms()函数遍历这些配置，创建并初始化虚拟机实例，处理DTB覆盖和内存分配。
1.12.1.5.3.AxVMCrateConfig 结构分析
该结构体封装了虚拟机的所有配置信息，包括基础信息、内核设置和设备规范。
1.12.1.5.3.1.配置字段语义与约束
字段	语义	默认值/约束
base.id	虚拟机唯一标识符	必填，正整数
base.name	虚拟机名称	必填，字符串
base.cpu_num	虚拟CPU数量	必填，正整数
kernel.entry_point	内核入口点	必填，物理地址
kernel.image_location	镜像位置	"memory" 或 "fs"
kernel.memory_regions	内存区域列表	每个区域包含基址、大小、标志和映射类型

